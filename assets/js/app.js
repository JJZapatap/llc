const cardsGrid=document.getElementById("cardsGrid"),emptyState=document.getElementById("emptyState"),searchInput=document.getElementById("searchInput"),topicFilter=document.getElementById("topicFilter"),clearFilters=document.getElementById("clearFilters"),toast=document.getElementById("toast");
function init(){fillTopics();renderStats(PROYECTOS);renderCards(PROYECTOS);searchInput.addEventListener("input",applyFilters);topicFilter.addEventListener("change",applyFilters);clearFilters.addEventListener("click",resetFilters)}
function fillTopics(){unique(PROYECTOS.map(p=>p.tema)).forEach(tema=>{const opt=document.createElement("option");opt.value=tema;opt.textContent=tema;topicFilter.appendChild(opt)})}
function renderStats(list){const temas=unique(PROYECTOS.map(p=>p.tema)).length,autores=unique(PROYECTOS.map(p=>p.autor)).length;setText("total",list.length);setText("temas",temas);setText("autores",autores);setText("totalHero",PROYECTOS.length);setText("temasHero",temas);setText("autoresHero",autores)}
function renderCards(list){cardsGrid.innerHTML="";if(!list.length){emptyState.classList.add("active");return}emptyState.classList.remove("active");list.forEach(item=>{const card=document.createElement("article");card.className="work-card";card.innerHTML=`<div class="cover"><img src="${escapeHtml(item.imagen)}" alt="Imagen representativa de ${escapeHtml(item.titulo)}"><span class="theme-pill">${escapeHtml(item.tema)}</span></div><div class="card-body"><h3>${escapeHtml(item.titulo)}</h3><p>${escapeHtml(item.descripcion)}</p><div class="meta"><div>${escapeHtml(item.icono)} <strong>Autoría:</strong> ${escapeHtml(item.autor)}</div><div>🎓 <strong>Programa:</strong> ${escapeHtml(item.programa)}</div><div>🔗 <strong>Publicación:</strong> GitHub Pages</div></div><div class="tags">${(item.etiquetas||[]).map(tag=>`<span class="tag">#${escapeHtml(tag)}</span>`).join("")}</div></div><div class="card-actions"><a class="btn primary" href="${escapeHtml(item.enlace)}" target="_blank" rel="noopener">Abrir trabajo</a><button class="btn copy" data-url="${escapeHtml(item.enlace)}">Copiar enlace</button></div>`;cardsGrid.appendChild(card)});document.querySelectorAll("[data-url]").forEach(btn=>{btn.addEventListener("click",()=>{copyText(btn.dataset.url);showToast("Enlace copiado");const original=btn.textContent;btn.textContent="Copiado ✅";setTimeout(()=>btn.textContent=original,1300)})})}
function applyFilters(){const text=normalize(searchInput.value),topic=topicFilter.value;const filtered=PROYECTOS.filter(item=>{const haystack=normalize([item.titulo,item.autor,item.programa,item.tema,item.descripcion,...(item.etiquetas||[])].join(" "));return(!text||haystack.includes(text))&&(topic==="todos"||item.tema===topic)});renderStats(filtered);renderCards(filtered)}
function resetFilters(){searchInput.value="";topicFilter.value="todos";renderStats(PROYECTOS);renderCards(PROYECTOS)}
function setText(id,value){const el=document.getElementById(id);if(el)el.textContent=value}
function unique(values){return[...new Set(values.filter(Boolean))].sort((a,b)=>a.localeCompare(b))}
function normalize(value){return String(value||"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"")}
function escapeHtml(value){return String(value||"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]))}
function copyText(text){if(navigator.clipboard){navigator.clipboard.writeText(text);return}const t=document.createElement("textarea");t.value=text;document.body.appendChild(t);t.select();document.execCommand("copy");t.remove()}
function showToast(msg){toast.textContent=msg;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1600)}
init();